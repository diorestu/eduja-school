Done. I generated exactly one image with `image_gen.imagegen`, but the tool did not expose a saveable PNG path in this environment, so I wrote the required failed `result.json` at the requested location.